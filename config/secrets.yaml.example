# Copy this file to secrets.yaml (in your Home Assistant config folder) and
# fill in your own values. secrets.yaml is already in Home Assistant's
# default .gitignore — never commit your real one.

# Wall display RPC endpoint — replace with your device's local IP
wall_display_rpc_url: http://192.168.1.XXX/rpc
